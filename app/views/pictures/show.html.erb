<div class="px-6 py-4">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
  <% end %>
  <%= render @picture %>
  <%= link_to "Back To Pictures", pictures_path, class: "float-right pb-4" %>
</div>
